<source>
  @type syslog
  port 5140
  protocol_type udp
  bind 0.0.0.0
  <parse>
    message_format auto
  </parse>
  tag "#{ENV['TAG']}"
</source>

<match **>
  @type sumologic
  endpoint "#{ENV['SUMO_HTTP_ENDPOINT']}"
</match>
