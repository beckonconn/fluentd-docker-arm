<source>
  @type syslog
  port 5140
  bind 0.0.0.0
  tag ${TAG}
</source>

<match *.*>
 @type sumologic
 datatype ${DATA_TYPE}
 endpoint ${SUMO_HTTP_ENDPOINT}
 log_format text
 source_category ${SOURCECATEGORY}
 open_timeout 10
</match>
