<source>
  @type syslog
  port 5140
  bind 0.0.0.0
  <transport udp>
  </transport>
  <parse>
    message_format auto
  </parse>
  tag "#{ENV['TAG']}"
</source>

<match "#{ENV['TAG']}.*">
  @type sumologic
  endpoint "#{ENV['SUMO_HTTP_ENDPOINT']}"
  log_format "#{ENV['LOG_FORMAT']}"
</match>
