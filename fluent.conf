<source>
  @type syslog
  port 5140
  <transport udp>
  </transport>
  bind 0.0.0.0
  <parse>
    message_format auto
  </parse>
  tag "#{ENV['TAG']}"
</source>

<match **>
  @type sumologic
  endpoint "#{ENV['SUMO_HTTP_ENDPOINT']}"
  log_format "#{ENV['LOG_FORMAT']}"
</match>
